[디스크 이미지 생성 및 루프백 디바이스 연결]
예시)
1. 이미지 생성
$ dd if=/dev/zero of=disk.img bs=1M count=32
2. 루프백 장치 확인
$ losetup -a
3. 비어있는 루프백 장치에 이미지 블럭 디바이스로 연결
$ sudo losetup /dev/loop0 disk.img
4. 루프백 장치 생성 확인
$ losetup -a
$ lsblk
5. 루프백 장치 제거
$ losetup -d /dev/loop0
6. 제거 확인
$ losetup -a
$ lsblk
